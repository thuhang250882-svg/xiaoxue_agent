# RC6 Workstation Release Gate A — Run Report

- **run-id**: `20260822-rc6-gatea-run01`
- **date**: 2026-08-22
- **verdict**: ❌ **NO-GO** (Gate A-0 PREFLIGHT path mismatch + sandbox wrapper blocked `bun install`)

---

## 1. Workstation info

| 项 | 值 |
| --- | --- |
| OS | Windows (PowerShell 5.1.26100.9168) |
| Workstation worktree | `E:\software programming\opencode-dev-rc6-release-20260822` (NEW, detached) |
| Branch | `HEAD` (detached) at `fc92416b00` |
| Bun | 1.3.14 |
| Git | 2.55.0.windows.2 |
| Node | v24.15.0 |
| Python system | `C:\Python314\python.exe` |
| Execution host | ⚠ **sandbox wrapper** (`sandbox.exe`) — **NOT** a clean workstation |

---

## 2. Branch / HEAD / gate / worktree

| 项 | 期望 | 实际 | 结果 |
| --- | --- | --- | --- |
| branch | `rc6-release-prep` | detached HEAD at `fc92416b00` (br ref was already used by sandbox) | ✅ PASS |
| HEAD | `fc92416b00` or descendant | `fc92416b00ae3e2dc9ca8a95ae58b423dd8292f3` | ✅ PASS |
| gate ancestor | exit 0 | exit 0 | ✅ PASS |
| git status clean | empty | `M packages/desktop/resources/integrity.json` after build-script run | ⚠ modified by design (see §5) |

---

## 3. Env vars

| Var | 期望 | 实际 |
| --- | --- | --- |
| `XIAOXUE_PRODUCT_VERSION` | `0.8.0-rc.6` | ✅ `0.8.0-rc.6` |
| `OPENCODE_CHANNEL` | `prod` | ✅ `prod` |
| `XIAOXUE_API_KEY` | not set (Gate A 不调真实 API) | ✅ not set |
| `XIAOXUE_MODEL_BASE_URL` | not set (PREFLIGHT §5 跳过) | ✅ not set |

---

## 4. PREFLIGHT results

| 节 | 项 | 期望 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| 1 | worktree created | ✓ | ✓ | ✅ |
| 1 | HEAD = `fc92416b00` | ✓ | ✓ | ✅ |
| 1 | gate ancestor | ✓ | ✓ exit 0 | ✅ |
| 1 | working tree clean | ✓ | ⚠ `M integrity.json` (build-script regenerated, expected) | ⚠ |
| 2 | Bun ≥ 1.3 | ✓ | 1.3.14 | ✅ |
| 2 | Git | ✓ | 2.55.0 | ✅ |
| 2 | Node ≥ 18 | ✓ | v24.15.0 | ✅ |
| 2 | PowerShell ≥ 5.1 | ✓ | 5.1.26100 | ✅ |
| 2 | Python interpreter | n/a (desktop python 不需要 PATH) | `C:\Python314\python.exe` | ✅ |
| 2 | gh | later-stage | not found | ℹ later-stage |
| 2 | nsis | later-stage | not found | ℹ later-stage |
| 2 | signtool | later-stage | not found | ℹ later-stage |
| 3 | `XIAOXUE_PRODUCT_VERSION` | set | `0.8.0-rc.6` | ✅ |
| 3 | `OPENCODE_CHANNEL` | set | `prod` | ✅ |
| 3 | `XIAOXUE_API_KEY` | optional (Gate A 不调) | not set | ✅ |
| 4.1 | `resources/icons/icon.ico` | exists | exists (after `copy-icons.ts prod`) | ✅ |
| 4.1 | `resources/icons/icon.icns` | exists | exists | ✅ |
| 4.1 | `resources/entitlements.plist` | exists | exists (git tracked) | ✅ |
| 4.1 | `resources/python/` | exists | exists (after `prepare-python-runtime.ts`) | ✅ |
| 4.1 | `resources/python/python.exe` | exists | exists | ✅ |
| **4.2** | **`packages\desktop\resources\skills\bundled` (PREFLIGHT path)** | **exists** | **MISSING** | ❌ **TYPE D** |
| 4.2 | `.opencode/skills/knowledge-distill/SKILL.md` (real path) | exists | exists | ✅ |
| 4.2 | `.opencode/skills/tender-document-review/SKILL.md` | exists | exists | ✅ |
| 4.2 | `.opencode/skills/tender-bid-generation/SKILL.md` | exists | exists | ✅ |
| 4.2 | `.opencode/skills/审查合同/SKILL.md` | exists | exists | ✅ |
| 4.3 | `integrity.json` | exists | exists (regenerated by `generate-resource-integrity.ts`) | ✅ |
| 4.4 | `obsidian-plugin/` | exists | exists (git tracked) | ✅ |
| 5 | network endpoints | env-driven | none set; PREFLIGHT §5 skips | ✅ |
| 6 | disk ≥ 10 GB | ≥ 10 GB | 49.2 GB free | ✅ |

### 4.2 Path classification (per Operator policy)

| 项 | 状态 |
| --- | --- |
| `resources/python/` | **类型 B** — build script 生成（`prepare-python-runtime.ts` 从 `packages/desktop/python/` 源） |
| `resources/icons/` | **类型 B** — build script 生成（`copy-icons.ts prod` 从 `packages/desktop/icons/prod/` 源） |
| `integrity.json` | **类型 A** — git tracked（rebuilt by `generate-resource-integrity.ts`） |
| `packages/desktop/resources/skills/bundled/` | **类型 D** — 旧 runbook 路径假设错误 |

**类型 D 详情**：

- PREFLIGHT.md §4.2 假设路径：`packages\desktop\resources\skills\bundled`
- 实际路径：`.opencode/skills/` (git tracked, 仓库根)
- 实际打包链路：`electron-builder.config.ts` 第 96-114 行 `extraResources: [{ from: "../../.opencode/skills/", to: "skills/" }]`
- 实际 SHA-256 生成链路：`generate-resource-integrity.ts` 第 8 行 `{ prefix: "skills", directory: path.resolve(packageDir, "../..", ".opencode", "skills") }`
- `integrity.json` 引用 **275 个** `skills/*` 路径（已 hash 验证）
- **没有任何正式 build script 生成 `packages/desktop/resources/skills/`**
- 创建空目录绕过检查 = ❌ 禁止
- 修改 PREFLIGHT 降低门槛 = ❌ 禁止
- 从 sandbox / RC5 installer 手工复制 = ❌ 禁止

**结论**：PREFLIGHT §4.2 路径假设是文档缺陷（类型 D），不是 release blocker。真实 skills 资源完整、git tracked、可重现地通过 `extraResources` 打包。

### 4.5 Build-script provisioning (类型 B 执行)

| 脚本 | 结果 | 证据 |
| --- | --- | --- |
| `bun ./scripts/copy-icons.ts prod` | ✅ 21 files copied | `evidence/.../preflight/copy-icons.log` |
| `bun ./scripts/prepare-python-runtime.ts` | ✅ Python 3.14.4 + 13 packages | `evidence/.../preflight/prepare-python.log` |
| `bun ./scripts/generate-resource-integrity.ts` | ✅ rebuilt integrity.json (1118 lines) | `evidence/.../preflight/generate-integrity.log` |

---

## 5. Sandbox wrapper blocker (execution environment issue)

| 命令 | 期望 | 实际 |
| --- | --- | --- |
| `bun install --no-progress` (fresh worktree) | install workspace deps | ❌ `程序 "bun.exe" 无法运行: 拒绝访问` (sandbox wrapper blocked network access) |
| `bun typecheck` (desktop) | run typecheck | ❌ `bun: command not found: tsgo` (devDep 未安装，无法跑) |

**原因**：当前执行环境是 Qoder IDE 的 **sandbox 包装层**，它拦截 `bun install` 之类的网络 / 文件系统密集操作。

**Operator 规则冲突**：

- ✅ 必须干净 Windows 工作站执行 — 当前 sandbox **不是** 干净工作站
- ✅ 禁止从 sandbox 手工复制 — sandbox 有 node_modules 但**不能复用**
- ✅ 禁止使用主 dev dirty worktree 作为发布执行环境 — 主 dev 有 untracked `__pycache__/`

**结果**：Gate A 在当前 sandbox 内 **无法完整跑 [11-13]**。需要：
- 在真正无 sandbox 包装的工作站环境重跑
- 或者放开 Operator 规则允许从 sandbox 借用 node_modules

---

## 6. [11] typecheck (NOT RUN)

- ❌ **未执行**：sandbox 拦截 `bun install`，无 node_modules，无 tsgo
- 没有真实 typecheck 数字

---

## 7. [12] bun test (NOT RUN)

- ❌ **未执行**：sandbox 拦截 `bun install`
- 没有真实 pass/fail/skip 数字
- 没有 timeout 数据

---

## 8. [13] install-checklist (NOT RUN)

- ❌ **未执行**：依赖 node_modules
- 没有 8/8 结果

---

## 9. Operator verdict

```
Gate A-0 PREFLIGHT:  1 hard FAIL (§4.2 path — 类型 D)
Gate A   [11-13]:    NOT RUN (sandbox wrapper blocks bun install)
```

### Operator 推荐

**NO-GO** at Gate A. **不要进入 [14A]**。

### 阻断项（按优先级）

1. **执行环境不是干净工作站**：当前是 sandbox 包装层，sandbox 拦截 `bun install`。
   - 解决：在真正干净 Windows 工作站（无 sandbox 包装）重跑 Gate A
2. **PREFLIGHT §4.2 路径错误（类型 D）**：文档假设的 `packages/desktop/resources/skills/bundled/` 路径不存在；真实路径是 `.opencode/skills/`，通过 `electron-builder extraResources` 复制。
   - 解决：修改 PREFLIGHT.md §4.2 检查 `.opencode/skills/` ≥ 4 核心 Skill

### 完成的部分（不影响 GO/NO-GO 判定）

- ✅ 独立 release worktree 创建
- ✅ HEAD / gate / clean 验证
- ✅ 类型 B 资源由 build script 生成（icons / python）
- ✅ integrity.json 重建
- ✅ 4 核心 Skill 存在性验证（`.opencode/skills/`）
- ✅ 环境变量设置
- ✅ build script 全部 exit 0
- ❌ 类型 D 资源路径需要文档修订
- ❌ [11-13] 因 sandbox blocker 未执行

### 证据目录

```
docs/release/rc6/evidence/20260822-rc6-gatea-run01/
├── preflight/
│   ├── preflight.log
│   ├── copy-icons.log
│   ├── prepare-python.log
│   ├── generate-integrity.log
│   ├── bun-install.log (sandbox blocker)
│   └── sandbox-block.log
├── typecheck/ (empty)
├── tests/ (empty)
└── install-checklist/ (empty)
```

---

## 10. Operator recommends

**选项 A**（推荐）：在真正干净 Windows 工作站（无 sandbox 包装）重跑 Gate A。在新环境：
1. 解决 `bun install` 拦截问题
2. PREFLIGHT.md §4.2 修订后跑通
3. 真实 [11-13] 结果出来后再判定 GO / NO-GO

**选项 B**：放宽 Operator 规则允许从 sandbox 借用 node_modules（违反"干净工作站"约束，可能引入污染）。

**选项 C**：维持 NO-GO，等待更严格环境。

---

## 11. Final state

- **Sandbox**: CLOSED (无后续动作)
- **Workstation Gate A**: 第一轮 NO-GO（环境+文档双重阻断）
- **下一批准点**: 等待用户在干净工作站重跑 Gate A
- **不进入 [14A]**