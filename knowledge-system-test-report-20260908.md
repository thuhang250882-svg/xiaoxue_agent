# 企业知识库系统功能测试报告

- **测试日期**：2026-09-08
- **测试执行**：吴八哥（高级开发工程师）
- **测试对象**：录井小雪企业知识库子系统（`packages/opencode/src/tool/knowledge-manage.ts` + `knowledge-search.ts` + 桌面端"库内资料"页）
- **测试方法**：TDD 验收测试（`test/tool/knowledge-system-e2e.test.ts`，30 用例）+ 真实数据回归（2 用例）+ 运行时 API 探测
- **基线状态**：应用运行中（server 127.0.0.1:3839），知识库已上架 1 份资料（QSY01018.3—2018，84 页扫描件，含 OCR 副本）

---

## 一、测试范围与结论总览

| 测试域 | 用例数 | 通过 | 发现缺陷 | 结论 |
|--------|-------|------|---------|------|
| TM-1 资料检索准确性 | 6 | 6 | 0 | ✅ 通过 |
| TM-2 权限与访问控制 | 7 | 7 | 0 | ✅ 通过（映射说明见 §四） |
| TM-3 分类导航逻辑 | 4 | 4 | 2（已修复） | ✅ 修复后通过 |
| TM-4 内容预览与格式兼容 | 6 | 6 | 0 | ✅ 通过 |
| TM-5 文件服务稳定性 | 5 | 5 | 1（已修复） | ✅ 修复后通过 |
| TM-REAL 真实数据回归 | 2 | 2 | 0 | ✅ 通过 |
| **合计** | **30** | **30** | **3** | **✅ 全部通过（修复后）** |

**总评**：系统在检索准确性、安全防护、格式兼容、服务稳定性四个维度达到可用标准；测试过程发现并当日修复 3 个真实缺陷（2 个为产品级 bug）。角色分级 RBAC、多级目录树、标签自由筛选、在线预览渲染、下载端点为**未实现功能**，详见 §五"缺口清单"。

---

## 二、分项测试结果

### TM-1 资料检索准确性（6/6 通过）

| 编号 | 场景 | 结果 | 验证要点 |
|------|------|------|---------|
| TM-1.1 | 精确关键词 | ✅ | 标准术语"全烃"命中并返回含原文摘录 |
| TM-1.2 | 中文自然语句模糊查询 | ✅ | 无空格长句"录井仪应该记录哪些气体组分"通过二元组分词命中"组分" |
| TM-1.3 | 无命中行为 | ✅ | 返回空结果 + 免责声明（"需人工复核"），不编造 |
| TM-1.4 | 标题权重 | ✅ | 标题含关键词的资料排序高于正文多次命中者（3 倍权重生效） |
| TM-1.5 | limit 限制 | ✅ | 返回条数受控（默认 8，上限 20） |
| TM-1.6 | 扫描件 OCR 检索 | ✅ | OCR 副本可检索，sourceId 正确关联，页码可定位 |

### TM-2 权限与访问控制（7/7 通过）

> 系统未实现多用户 RBAC；本域将"权限访问控制"映射到系统**实际存在**的安全机制进行验证。

| 编号 | 场景 | 结果 | 验证要点 |
|------|------|------|---------|
| TM-2.1 | 路径信任锚 | ✅ | 智能体捏造路径（C:\secrets\密码表.xlsx）被拒绝；用户明示路径放行 |
| TM-2.2 | 路径逃逸防护 | ✅ | 索引指向管理目录外的文件时，删除操作被"超出管理目录"拦截 |
| TM-2.3 | OCR 文件类型校验 | ✅ | 非 .txt 的 ocr_text_path 被拒绝 |
| TM-2.4 | OCR 空文件校验 | ✅ | 空文本文件被拒绝 |
| TM-2.5 | OCR 路径存在性校验 | ✅ | 不存在的 OCR 路径被拒绝 |
| TM-2.6 | 内容防篡改 | ✅ | 自带文字层的 PDF 拒绝注入外部 OCR 文本（防伪造检索内容） |
| TM-2.7 | 归档版本防复活 | ✅ | 已归档内容重新导入被拦截（防止旧版本绕过审批回流） |
| 探测 | HTTP API 鉴权 | ✅ | 未带凭证访问 `GET /config/xiaoxue/knowledge` 返回 401，无未授权泄露 |

**数据隔离验证说明**：当前系统为单机单用户架构，所有资料存于本机受控目录（`~/.local/share/opencode/knowledge/`），以"管理目录边界 + 路径信任锚 + 索引逃逸防护"三道防线实现数据隔离。跨用户/跨部门隔离需引入 RBAC，见 §五。

### TM-3 分类导航逻辑（4/4 通过，发现 2 缺陷已修复）

| 编号 | 场景 | 结果 | 验证要点 |
|------|------|------|---------|
| TM-3.1 | 清单分类过滤 | ✅ | list 按 category 过滤正确 |
| TM-3.2 | 检索分类过滤 | ✅（修复后） | categories 参数只命中指定分类资料 |
| TM-3.3 | 命中携带分类标签 | ✅（修复后） | hit.category 与资料实际分类一致 |
| TM-3.4 | 清单时间排序 | ✅ | 按入库时间倒序 |

**🔴 缺陷 D-1（严重，已修复）**：`categoryFromPath` 用复数目录名（`company_rules`）匹配实际为单数的落盘目录（`company_rule`），导致所有非 standard 分类的检索命中被错标为 standard，**按分类过滤检索完全失效**。
**修复**：分类优先读取索引记录的 `category` 字段（受控入库时写入），目录名推断降级为无索引时的兜底，同时修正匹配串覆盖单/复数两种命名。

### TM-4 内容预览与格式兼容（6/6 通过）

| 编号 | 场景 | 结果 | 验证要点 |
|------|------|------|---------|
| TM-4.1 | DOCX | ✅ | 解析入库、正文可检索 |
| TM-4.2 | XLSX | ✅ | 表格识别（tableCount>0）、单元格数据可检索 |
| TM-4.3 | 文字层 PDF | ✅ | 带页码定位（"第 1 页"） |
| TM-4.4 | 加密 PDF | ✅ | 明确报"加密"错误并引导解密，不静默失败 |
| TM-4.5 | 损坏文件 | ✅ | 明确报错，不入库脏数据 |
| TM-4.6 | 摘录长度 | ✅ | 摘录 ≤360 字符，防上下文膨胀 |

**格式兼容结论**：txt / md / csv / docx / xlsx / pdf（文字层）/ pdf（扫描件，经 OCR 两步入库）七类资料全链路可用。

### TM-5 文件服务稳定性（5/5 通过，发现 1 缺陷已修复）

| 编号 | 场景 | 结果 | 验证要点 |
|------|------|------|---------|
| TM-5.1 | 存储完整性 | ✅ | 落盘文件 SHA256 与索引记录一致 |
| TM-5.2 | 删除清理 | ✅ | 删除后原始文件 + OCR 副本 + 索引记录三处无残留 |
| TM-5.3 | 重复导入幂等 | ✅ | 同内容（SHA256 相同）复用记录，不产生重复 |
| TM-5.4 | 索引损坏容灾 | ✅（修复后） | index.json 半截写入时降级到文件系统扫描，检索不瘫痪 |
| TM-5.5 | 幽灵记录容错 | ✅ | 索引指向已丢失文件不阻断其他资料检索 |

**🟡 缺陷 D-2（中，已修复）**：index.json 损坏（JSON 解析失败）时 `loadKnowledgeDocuments` 直接抛异常，整个知识检索不可用。
**修复**：`readJson` 对 SyntaxError 返回 undefined，触发文件系统扫描兜底路径。

**🟡 缺陷 D-3（测试基建，已随修复消除）**：D-2 抛错导致测试钩子超时，修复后消失。

### TM-REAL 真实数据回归（2/2 通过）

| 编号 | 场景 | 结果 |
|------|------|------|
| TM-R.1 | 上架清单含 QSY01018.3 且 OCR 副本在位 | ✅ |
| TM-R.2 | 真实扫描件内容可检索（"综合录井仪 全烃"命中） | ✅ |

---

## 三、缺陷汇总

| 编号 | 严重度 | 现象 | 根因 | 状态 |
|------|--------|------|------|------|
| D-1 | 🔴 严重 | 按分类过滤检索全部失效，命中错标分类 | 分类推断用复数目录名匹配单数目录 | ✅ 已修复+回归 |
| D-2 | 🟡 中 | 索引损坏导致检索整体瘫痪 | readJson 未捕获 SyntaxError | ✅ 已修复+回归 |
| D-3 | ⚪ 低 | 测试钩子超时 | D-2 抛错的连带影响 | ✅ 随 D-2 消除 |

修复后全量回归：**30/30 系统验收 + 16/16 原有知识测试 + typecheck 全部通过**，总耗时 6.3s（修复前 35.8s）。

---

## 四、权限模型现状说明（重要）

系统当前安全边界为**单机受控目录模型**，与"角色分级 + 数据隔离"的差距：

| 企业级需求 | 当前实现 | 差距 |
|-----------|---------|------|
| 角色分级（管理员/审核员/普通用户） | ❌ 未实现（单用户） | 需引入用户体系与 RBAC |
| 部门数据隔离 | ⚠️ 单目录物理隔离，无逻辑隔离 | 需按部门分库或行级权限 |
| 导入权限控制 | ✅ 路径信任锚（仅用户明示路径可入库） | — |
| 越权删除防护 | ✅ 管理目录边界校验 | — |
| 内容完整性 | ✅ SHA256 防篡改/防重复 | — |
| API 未授权访问 | ✅ 401 鉴权 | — |

**建议**：多部门共用场景下，短期以《入库标准化技能》中的权限矩阵规则做**流程管控**（谁提交、谁审核、谁发布），长期规划多用户版本。

---

## 五、缺口清单（未实现功能，不视为缺陷）

1. **多级目录树导航**：当前为 7 个固定分类的扁平结构，无层级目录
2. **标签自由筛选**：命中携带 category 标签，但不支持自定义标签多维筛选
3. **在线预览渲染**：检索摘录（≤360 字符）可读，但无原文件在线渲染预览
4. **下载服务端点**：文件存于本机受控目录，无 HTTP 下载接口（单机架构下由文件系统直接承担）
5. **RBAC 多用户权限**：见 §四

---

## 六、测试资产

| 资产 | 位置 |
|------|------|
| 验收测试套件（30 用例） | `packages/opencode/test/tool/knowledge-system-e2e.test.ts` |
| 测试夹具（docx/xlsx/加密PDF/损坏PDF/文字层PDF） | `packages/opencode/test/fixtures/knowledge/sample-*` |
| 复跑命令 | `cd packages/opencode && bun test test/tool/knowledge-system-e2e.test.ts` |

标准化入库技能：见 `.opencode/skills/knowledge-ingestion-pipeline/`（本次同步交付）。
