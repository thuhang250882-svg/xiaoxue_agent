# 录井小雪桌面应用 — 代码优化工作 Handoff 文档

- **日期**：2026-09-08
- **交接方**：吴八哥（高级开发工程师）
- **承接方**：Codex / 后续开发者
- **主仓库**：`E:\software programming\opencode-desktop-feedback`（活跃开发）
- **同步仓库**：`E:\software programming\opencode-dev`（双仓同步目标，见 §4.2）
- **当前状态**：应用运行中（dev 模式，server 127.0.0.1 动态端口），全部测试绿，**改动未提交**（100 个文件 dirty，含本次与此前多轮修复）

---

## 1. 已优化模块及具体改动摘要

### 1.1 pdfkit-py 技能：OCR 后端统一（`.opencode/skills/pdfkit-py/`）

| 文件 | 改动 |
|------|------|
| `scripts/pdfkit/ocr_backend.py` **新建** | 统一 OCR 后端：RapidOCR 主通道（引擎缓存 `get_rapidocr()`，多页文档只加载一次模型）+ pytesseract 兜底。提供 `ocr_page_text`（纯文本）与 `ocr_page_blocks`（带 PDF 坐标）两个接口，坐标换算统一在后端完成 |
| `scripts/pdfkit/commands/extract_text.py` | `_ocr_extract_page_text()` 改走 `ocr_backend.ocr_page_text`；参数 help 更新 |
| `scripts/pdfkit/commands/ocr_locate.py` | handler 改走 `ocr_backend.ocr_page_blocks`，删除内联 pytesseract 逻辑；保留后半段精确/连续词匹配逻辑不变 |
| `SKILL.md` | 三处描述更新（可选依赖、extract_text 参数表、ocr_locate），说明"RapidOCR 优先，tesseract 兜底" |

**根因背景**：SKILL.md 一直承诺"内置 RapidOCR"但四个 OCR 命令全部只 import pytesseract——承诺与实现脱节。打包 Python（`packages/desktop/resources/python/`）实际内置 rapidocr 3.9.2 + 3 个 ONNX 模型（PP-OCRv6 det/rec small + cls），离线可用。

**关键坑（已修）**：RapidOCR 返回坐标是 numpy float32，`round()` 不改变类型 → JSON 序列化抛 `Object of type float32 is not JSON serializable`，必须显式 `float()`。

### 1.2 知识管线：扫描件 PDF 端到端支持（`packages/opencode/src/tool/`）

| 文件 | 改动 |
|------|------|
| `knowledge-manage.ts` | ① `KnowledgeRecord` 新增 `textPath?: string`；② Parameters 新增 `ocr_text_path`（扫描件 OCR 文本路径）；③ 新增 `parseSourceWithOcrFallback()`——捕获 `PDF_NO_EXTRACTABLE_TEXT` 走 OCR 降级，自带文字层却传 ocr_text_path 时拒绝（防篡改）；④ import/update 写入 OCR 副本 `${filePath}.txt`；⑤ remove 同时删原件+副本（含越界校验）；⑥ 工具 description 写明扫描件两步入库 |
| `knowledge-search.ts` | ① `scanKnowledgeRoot` 与 `metadataForFile` 优先 `textPath ?? filePath`；② `metadataForFile` 注入 `category` 到文档元数据；③ `isManagedIndexRecord` 类型扩展 `category`/`textPath` |
| `test/tool/knowledge-manage.test.ts` | 新增 5 个扫描件场景（导入+检索、无OCR引导、多余OCR拒绝、多资料拒绝），14/14 过 |

**架构说明**：`parseDocument` 对无文字层 PDF 直接抛错（`PDF_NO_EXTRACTABLE_TEXT`），不做降级。因此扫描件支持做在 knowledge-manage 层：原 PDF 归档保存（库内资料显示不变），OCR 文本副本作为可检索内容。

### 1.3 检索分类 bug 修复（严重级，测试驱动发现）

**缺陷**：`categoryFromPath` 用复数目录名（`company_rules`）匹配单数落盘目录（`company_rule`）→ 永不命中 → 所有非 standard 分类的检索命中被错标为 standard，**按分类过滤检索完全失效**。

**修复**（`knowledge-search.ts`）：
- 分类优先读索引记录的 `category` 字段（受控入库时写入，`metadataForFile` 注入文档元数据）
- 目录名推断降级为无索引时的兜底，匹配串改为单数（单数是复数的子串，两种命名都覆盖）

### 1.4 检索容灾修复（中等级）

**缺陷**：`readJson` 未捕获 `SyntaxError` → index.json 损坏（半截写入）时 `loadKnowledgeDocuments` 整体抛异常。
**修复**：`readJson` 对 SyntaxError 返回 undefined，触发 `scanKnowledgeFiles` 文件系统扫描兜底。

### 1.5 系统功能测试套件（新建）

| 资产 | 位置 |
|------|------|
| 验收测试 30 用例（5 域 + 真实数据回归 2 例） | `packages/opencode/test/tool/knowledge-system-e2e.test.ts` |
| 测试夹具（docx/xlsx/加密PDF/损坏PDF/文字层PDF） | `packages/opencode/test/fixtures/knowledge/sample-*` |
| 测试报告 | `E:\software programming\opencode-dev\knowledge-system-test-report-20260908.md` |

复跑命令：`cd packages/opencode && bun test test/tool/knowledge-system-e2e.test.ts test/tool/knowledge-manage.test.ts test/tool/knowledge-retrieval-eval.test.ts` → **46/46 通过，约 4.3s**。

### 1.6 标准化入库技能（新建，两仓库已装）

`.opencode/skills/knowledge-ingestion-pipeline/`：
- `SKILL.md`——五环节路由 + 铁律（受控通道、信任锚、原件不可改、扫描件两步、审批前不称"已上架"）+ 快速参考表 + 交付物清单
- `references/collection-template.md`——资料登记表模板 + 来源授权红线（涉密拒收）
- `references/format-compliance-checklist.md`——七格式支持矩阵 + OCR 抽查标准（目视 ≥95%）+ 退回原因
- `references/taxonomy-config.md`——七分类判定顺序 + 标题命名规范
- `references/permission-matrix.md`——提交/审核/发布三角色矩阵 + 五条设定规则
- `references/publish-approval.md`——审批链 + 发布前检查单 + 入库回执格式 + 版本/下架/回滚规则

### 1.7 配置登记（三处，缺一不可）

| 文件 | 改动 |
|------|------|
| `configs/xiaoxue/skills.yaml` | 新增 `knowledge_ingestion_pipeline` 条目（agent: knowledge, tool: knowledge_manage） |
| `configs/xiaoxue/router.md` | 知识库导入/入库/审批流程行指向 `knowledge-ingestion-pipeline`；路由边界第 5 条强制走受控入库 |
| `configs/xiaoxue/rc-release-profile.json` | `INTERNAL_DEPENDENCIES` 加入新技能；`rc.skillCount` 10→11；`platformEffectiveSkillCount` 28→29 |
| `configs/xiaoxue/knowledge_query.md` | 子代理提示词：扫描件两步入库流程 + 禁止只 OCR 不入库 |

### 1.8 此前已完成（本次会话之前，均已在运行中的应用生效）

- EPIPE 异常风暴静默（`packages/desktop/src/main/index.ts`）
- 思考过程折叠、长正文全量渲染（`packages/session-ui/src/components/message-part.tsx`）
- 数字人窗口光标保持（`packages/desktop/src/xiaoxue-pet/XiaoxuePetWindow.tsx`）
- 权限基础设施：findLast 语义下白名单后置、子代理继承排除 catch-all deny（`agent.ts` / `subagent-permissions.ts`）
- 记忆首轮触发复盘（`xiaoxue/memory.ts`）
- 知识库直览页 + `/config/xiaoxue/knowledge` 端点
- 拖拽意图选择弹窗（`dialog-dropped-file-choice.tsx`）
- pdfjs DOMMatrix 桩 + fake worker 注入（`document_engine/parsers/pdf_parser.ts` + `packages/opencode/src/pdfjs-worker.d.ts`）
- 删除私有"自动接受权限"设置

---

## 2. 任务完成状态与待办事项

### ✅ 已完成（本日）

- [x] OCR 统一后端（RapidOCR 主 + pytesseract 兜底），真实 84 页扫描件 3 分 7 秒全量提取
- [x] QSY01018.3—2018 实际入库：`KN-E72A4A4DF582`（96 段，PDF + OCR 副本双落盘，SHA256 校验一致）
- [x] 知识管线扫描件端到端支持（import/update/remove/list/search 全生命周期）
- [x] 系统功能测试 30 用例 + 报告，3 缺陷发现并当日修复
- [x] 标准化入库技能安装 + 三处配置登记，应用重启生效

### ⏳ 待办（按优先级）

| 优先级 | 事项 | 说明 |
|--------|------|------|
| **P0** | **提交代码** | 100 个文件 dirty 未提交。建议按模块分批 commit（pdfkit OCR / 知识管线 / 测试套件 / 技能+配置），避免一次巨无霸提交 |
| P1 | 端到端复验"导入知识库"路由 | 用户对小雪说"导入知识库"，确认路由到 knowledge agent 走受控入库（新 router 规则），不再绕道 office/obsidian。本轮只做了单元级验证 |
| P1 | 观察记忆积累 | 首轮复盘修复后，观察用户画像/项目记忆是否开始生成（`~/.local/share/opencode/xiaoxue/memory/xiaoxue-memory.sqlite`） |
| P2 | 表 60/61 结构化提取 | QSY01018.3 中数据表为扁平文本；需要时 `pdfkit.py extract_table --method lattice` 对指定页二次处理 |
| P2 | `--format blocks` 触发 OCR | `extract_text.py:149` 仅 `fmt=="text"` 走 OCR；blocks 坐标需求出现时再补 |
| P3 | 缺口清单五项 | RBAC 多用户、多级目录树、自由标签、在线预览渲染、下载端点——见测试报告 §五，均未实现 |

---

## 3. 下一步可执行目标清单

按顺序执行，每步含验证标准：

1. **分批提交**（P0）
   ```bash
   cd "E:/software programming/opencode-desktop-feedback"
   # 批1: git add .opencode/skills/pdfkit-py/ && git commit  # OCR 后端
   # 批2: git add packages/opencode/src/tool/knowledge-*.ts packages/opencode/test/tool/ packages/opencode/test/fixtures/knowledge/ && git commit
   # 批3: git add .opencode/skills/knowledge-ingestion-pipeline/ configs/xiaoxue/ && git commit
   # 批4: 其余（app 组件、desktop、document_engine —— 注意其中混有其他工作线的改动，先 git diff 逐文件确认归属）
   ```
   验证：`git status` 干净；每批提交后 `bun test` 相关套件仍绿。

2. **路由端到端复验**（P1）
   - 重启应用（见 §4.3），对话输入"导入知识库"+ 一个 PDF 路径
   - 验证：日志/工具调用显示 `knowledge_manage` import（而非 obsidian archive）；`knowledge_manage list` 出现新记录
   - 扫描件场景：确认小雪自动走两步入库（pdfkit OCR → import + ocr_text_path）

3. **记忆功能观察**（P1）
   - 正常使用 2-3 个新会话后，查 `xiaoxue-memory.sqlite` 是否有新记忆条目
   - 无生成则查 `packages/opencode/src/xiaoxue/memory.ts` 的 reviewPrompt 触发链路

4. **（可选）表结构化**（P2）
   ```bash
   XIAOXUE_PYTHON="packages/desktop/resources/python/python.exe" PYTHONNOUSERSITE=1 \
     packages/desktop/resources/python/python.exe .opencode/skills/pdfkit-py/scripts/pdfkit.py \
     extract_table --input "<QSY PDF路径>" --pages '[68,69]' --method lattice
   ```

---

## 4. 项目技术上下文与依赖约束

### 4.1 架构概览

- **Electron + opencode 桌面应用**（"录井小雪"，面向录井工程分公司的企业级知识智能体）
- 关键链路：Electron 主进程 → utilityProcess sidecar → `virtual:opencode-server`（解析为 `packages/opencode/dist/node/node.js` **构建产物**，非源码直跑）
- **dev 模式注意**：改 opencode 源码后必须重启；`bun run dev` 的 predev 会执行 `bun script/build-node.ts` 重新构建
- 前端：SolidJS（信号/资源模式）；工具层：Effect + Schema

### 4.2 双仓库同步约定

- `opencode-desktop-feedback` = 活跃开发仓；`opencode-dev` = 同步目标仓
- **两仓库 `packages/desktop/resources/python/` 是同一物理目录（junction）**——改一边即两边生效
- 本次已同步的文件清单（feedback → dev）：`ocr_backend.py`、`extract_text.py`、`ocr_locate.py`、`pdfkit-py/SKILL.md`、`knowledge-manage.ts`、`knowledge-search.ts`、`knowledge-manage.test.ts`、`knowledge-system-e2e.test.ts`、`fixtures/knowledge/sample-*`、`knowledge_query.md`、`router.md`、`skills.yaml`、`rc-release-profile.json`、`document_engine/parsers/pdf_parser.ts`、knowledge-ingestion-pipeline 技能全目录
- **新发现**：opencode-dev 的 `document_engine/exporters/` 还有多个文件落后于 feedback（属其他工作线，本次未动，交接时知悉）

### 4.3 启动/停止命令

```bash
# 启动（必须清除宿主注入的 ELECTRON_RUN_AS_NODE）
cd "E:/software programming/opencode-desktop-feedback/packages/desktop" && unset ELECTRON_RUN_AS_NODE && bun run dev
# 停止
taskkill //IM electron.exe //F; taskkill //IM electron-vite.exe //F
```
启动约 2.5-3 分钟（含 opencode 重建）。验证：`tasklist | grep -i electron` 出现多进程；日志 `~/.local/share/opencode/log/opencode.log` 出现 `message=init`；EPIPE 计数应为 0。

### 4.4 打包 Python 运行时（硬约束）

- 路径：`packages/desktop/resources/python/python.exe`（3.14.4）
- 技能规范**禁止**系统 Python；运行 pdfkit 必须：
  ```bash
  XIAOXUE_PYTHON="<上述python.exe绝对路径>" PYTHONNOUSERSITE=1 <python.exe> pdfkit.py <command>
  ```
  （pdfkit 的 `_require_office_runtime()` 校验三项：XIAOXUE_PYTHON 存在、与 sys.executable 一致、user-site 隔离）
- 内置：fitz/PyMuPDF、rapidocr 3.9.2（+3 个 ONNX 模型，`site-packages/rapidocr/models/`）、docx、openpyxl、pandas 等

### 4.5 测试与检查

```bash
cd packages/opencode
bun test test/tool/                       # 全部工具测试
bun run typecheck                         # tsgo --noEmit
```

### 4.6 权限系统语义（改权限规则时必读）

- OpenCode `evaluate` 用 `findLast`（**最后匹配胜出**）→ 白名单必须声明在 `"*": "deny"` **之后**
- 子代理权限继承：`deriveSubagentSessionPermission` 合并顺序 `[子代理自身权限, 会话权限]`，继承过滤已排除 catch-all deny

---

## 5. 关键决策与注意事项

### 5.1 关键设计决策（不要回退）

1. **扫描件 = 原件归档 + OCR 副本检索**（而非"只存 OCR 文本"）：`filePath` 指向原 PDF（库内资料/下载语义不变），`textPath` 指向 OCR 副本（检索语义）。删除/更新/归档必须双文件同步操作——已有测试覆盖。
2. **分类以索引记录为准**：`category` 写进 index.json 与文档元数据，目录名推断只是无索引兜底。不要把推断改回主路径（D-1 的教训）。
3. **OCR 双通道**：RapidOCR 永远优先（离线、内置、中文准确），pytesseract 只是兜底。不要让 tesseract 成为唯一路径。
4. **入库只能走 `knowledge_manage`**：路由与提示词已三重强调（router.md / knowledge_query.md / 技能铁律）。任何"归档替代入库"的旁路都是回归。
5. **信任锚不可放松**：`userMentionedPaths` 要求路径逐字出现在用户消息文本中——这是防智能体捏造路径导任意文件的核心防线。

### 5.2 陷阱清单（前任踩过的坑）

| # | 陷阱 | 规避 |
|---|------|------|
| 1 | **新增技能不登记 rc-release-profile.json → predev 启动直接崩**（`Platform Skill catalog mismatch: declared=28 active=29`） | 加技能必须同步三处：skills.yaml + router.md + rc-release-profile.json（含两个计数字段） |
| 2 | numpy float32 过 JSON 序列化炸 | RapidOCR 输出显式 `float()` |
| 3 | `parseDocument` 对无文字层 PDF **抛错**而非返回空 | 扫描件逻辑必须 catch `PDF_NO_EXTRACTABLE_TEXT`（错误码在 `domains/shared/errors.ts`） |
| 4 | bun 测试里 import 相对路径基于脚本位置而非 cwd | 跨目录脚本用绝对路径 import |
| 5 | dev sidecar 跑 dist 产物，改源码"看起来没生效" | 重启（predev 自动重建） |
| 6 | Bash 工具下 `cmd /c` / `powershell` 被安全策略拦截 | 用纯 bash 语法或 PowerShell 专用工具 |
| 7 | pdfjs-dist v6 在 utilityProcess 被误判为浏览器（`process.type === "utility"`）| 已注入 `globalThis.pdfjsWorker` fake worker + DOMMatrix/Path2D 桩，勿删 `ensurePdfjsWebApiStubs`/`ensurePdfjsMainThreadWorker` |
| 8 | Edit 工具对含多字节字符文件偶发"字符串未找到" | 先 `cat -A` 检查不可见字符，或整文件 Write 重写 |

### 5.3 业务事实（验收基线）

- 知识库当前 1 份上架资料：`KN-E72A4A4DF582`（QSY01018.3—2018，84 页扫描件，OCR 55,227 字符 / 96 段，实测检索命中）
- QSY01018.3 页 1/5/81/82 为**真空白页**（像素统计 0 深色像素），非漏识别，无需补跑
- 35 张事实卡（QSY 01128 蒸馏）在 Obsidian `D:\知识库\06-日常工作管理\智能体协作\`，属知识资产层，不在受控索引内

### 5.4 未实现能力（用户已知悉，勿当作 bug）

RBAC 多用户、多级目录树、自由标签筛选、原文件在线预览渲染、HTTP 下载端点——均已写入测试报告缺口清单 §五。当前安全模型为"单机受控目录 + 三道技术防线 + 流程级权限矩阵（新技能的环节 4）"。

---

## 附：本次会话全部验证记录

| 验证项 | 结果 |
|--------|------|
| RapidOCR 端到端（合成中文图片） | 一字不差 |
| pdfkit extract_text --ocr_fallback（合成扫描件） | 2/2 页全提取 |
| pdfkit ocr_locate --text 定位 | 精确命中，置信度 97 |
| 真实 84 页扫描件全量 OCR | 55,227 字符 / 187s |
| QSY01018.3 入库 + 检索（3 组查询） | 全命中，页码定位 |
| knowledge-manage 测试 | 14/14 |
| knowledge-system-e2e 验收 | 30/30（修复后） |
| knowledge-retrieval-eval | 2/2 |
| 双仓库全量回归 | 46/46 |
| typecheck（两仓库） | 通过 |
| 应用重启 | 0 EPIPE，server 正常 |
